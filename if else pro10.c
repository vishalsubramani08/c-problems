// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a;
    scanf("%d",&a);
    if(a>=100 && a<=999)
    printf("three digit number ");
    else    
    printf("not three digit number  ");
    return 0;
}