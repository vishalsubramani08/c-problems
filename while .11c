#include <stdio.h>

int main() {
int n , hour ,sum=0, count=0;
scanf("%d",&n);
while(n>=1){
    scanf("%d",&hour);
    sum+=hour;
    if(hour>2){
        count++;
    }
    n--;
}
printf("total delay hours:%d\n",sum);
printf("delayed days:%d",count);
return 0;
}